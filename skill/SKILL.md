---
name: codespaces-jupyter-level1
description: Use in CODESPACES_jyperter to census, modernize, repair, innovate, or perpetuate notebook/CLI capability; execute the Level-1 runtime; coordinate artifact/property worker receipts; and run measured PCA or Bradley-Terry diagnostics without promoting exploratory output to authoritative QPS evidence.
---

# Codespaces Jupyter Level 1

1. Read `LEVEL1.md` and `level1/ssot.json`.
2. Run `python level1/runtime.py census`, then `python level1/runtime.py mip`.
3. Reuse `src/zip_pipeline.py` and `tools/extract_slide_previews.py`; do not clone healthy utilities.
4. Run `python level1/runtime.py orchestrate` for blocks, agents, role and federation targets.
5. PCA requires real measured numeric rows; BT requires explicit winner/loser comparisons. Missing input must DEFER.
6. Run `python level1/runtime.py self-test` before any Level-1.0 claim. Synthetic fixtures are engine tests only, never project evidence.
7. The worker may emit exact-SHA environment/result receipts but may not assign QPS engineering/compliance authority to notebook or exploratory results.
